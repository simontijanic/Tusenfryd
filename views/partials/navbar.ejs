<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Tusenfryd</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item">
          <a class="nav-link" href="/attractions">Attraksjoner</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/faq">FAQ</a>
        </li>
        <% if (session && session.isAdmin) { %>
          <li class="nav-item">
            <a class="nav-link" href="/admin">Admin panel</a>
          </li>
          <li class="nav-item d-flex align-items-center">
            <form action="/auth/logout" method="POST" style="display:inline; margin-bottom:0;">
              <button type="submit" class="nav-link btn btn-outline-danger btn-sm">Logg ut 🔒</button>
            </form>
          </li>
        <% } else if (session && session.isLoggedIn) { %>
          <li class="nav-item d-flex align-items-center">
            <form action="/auth/logout" method="POST" style="display:inline; margin-bottom:0;">
              <button type="submit" class="nav-link btn btn-outline-danger btn-sm">Logg ut 🔒</button>
            </form>
          </li>
        <% } else { %>
          <li class="nav-item">
            <a class="nav-link" href="/auth/login">Logg inn</a>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
