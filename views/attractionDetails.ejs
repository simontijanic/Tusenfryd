<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= attraction.name %> - Detaljer</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <%- include('partials/navbar', { session: session }) %>
    <div class="container mt-4">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card shadow">
                    <div class="card-body text-center">
                        <img 
                            src="/images/<%= attraction.image || 'default.jpg' %>" 
                            alt="<%= attraction.name %>" 
                            class="mb-4 shadow-lg rounded" 
                            style="width:100%; max-width:600px; height:350px; object-fit:cover; border-radius: 1rem; border: 4px solid #eee;"
                        >
                        <h2 class="card-title mb-3"><%= attraction.name %></h2>
                        <h6 class="card-subtitle mb-2 text-muted">Åpningstid: <%= attraction.openingTime %> - <%= attraction.closingTime %></h6>
                        <p class="mb-1"><strong>Ventetid:</strong> <%= attraction.waitTime %> min</p>
                        <p class="mb-1"><strong>Status:</strong> <% if (attraction.isOpen) { %><span class="badge bg-success">Åpen</span><% } else { %><span class="badge bg-danger">Stengt</span><% } %></p>
                        <p class="mb-1"><strong>Kapasitet i kø:</strong> <%= attraction.queue ? attraction.queue.length : 0 %> / <%= attraction.queueCapacity || 20 %></p>
                        <% if (typeof queuePosition === 'number' && queuePosition !== null) { %>
                            <p class="mb-1"><strong>Din plass i køen:</strong> <%= queuePosition %></p>
                        <% } %>
                        <p class="mt-3"><strong>Beskrivelse:</strong><br> <%= attraction.description || 'Ingen beskrivelse.' %></p>
                        <a href="/attractions" class="btn btn-secondary mt-3">Tilbake til attraksjoner</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <%- include('partials/footer') %>
</body>
</html>
